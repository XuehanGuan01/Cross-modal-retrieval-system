┌─────────────┐     1. 上传图片     ┌─────────────┐
│  前端组件   │ ──────────────────> │ 后端 API   │
└─────────────┘                     └─────────────┘
                                      │
                                      │ 2. 调用 encode_single_image
                                      ▼
┌─────────────┐     7. 返回结果     ┌─────────────┐
│  前端组件   │ <───────────────── │ 后端 API   │
└─────────────┘                     └─────────────┘
                                      │
                                      │ 6. 转换为文本结果
                                      ▼
                             ┌──────────────────┐
                             │ 元数据处理       │
                             └──────────────────┘
                                      │
                                      │ 5. 构建结果列表
                                      ▼
                             ┌──────────────────┐
                             │ SearchEngine     │
                             │ (FAISS 索引)     │
                             └──────────────────┘
                                      │
                                      │ 4. 执行向量搜索
                                      ▼
                             ┌──────────────────┐
                             │ 向量库 (FAISS)   │
                             └──────────────────┘
                                      │
                                      │ 3. 图片编码为向量
                                      ▼
                             ┌──────────────────┐
                             │ CN-CLIP 模型     │
                             └──────────────────┘


文搜图

┌─────────────┐     1. 输入文本     ┌─────────────┐
│  前端组件   │ ──────────────────> │ 后端 API   │
└─────────────┘                     └─────────────┘
                                      │
                                      │ 2. 调用 encode_single_text
                                      ▼
┌─────────────┐     6. 返回结果     ┌─────────────┐
│  前端组件   │ <───────────────── │ 后端 API   │
└─────────────┘                     └─────────────┘
                                      │
                                      │ 5. 构建结果列表
                                      ▼
                             ┌──────────────────┐
                             │ SearchEngine     │
                             │ (FAISS 索引)     │
                             └──────────────────┘
                                      │
                                      │ 4. 执行向量搜索
                                      ▼
                             ┌──────────────────┐
                             │ 向量库 (FAISS)   │
                             └──────────────────┘
                                      │
                                      │ 3. 文本编码为向量
                                      ▼
                             ┌──────────────────┐
                             │ CN-CLIP 模型     │
                             └──────────────────┘